#ifndef _UTILS_H_
#define _UTILS_H_

#include <algorithm>
#include <vector>
using namespace std;

bool cmp(const vector<double> a, const vector<double> b);
double dist_func(double a, double b);

#endif
